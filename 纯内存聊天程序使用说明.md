# 使用说明：纯内存聊天程序

## 📖 程序介绍

`pure_memory_chat.py` 是一个简单的 Azure OpenAI 聊天程序，具有以下特点：

- ✨ **纯内存记忆**：在对话过程中记住所有之前的对话内容
- 🔄 **连续对话**：AI 能够参考之前的对话内容回答问题
- 🗑️ **记忆管理**：可以清空记忆，查看记忆状态
- 📱 **简单易用**：命令行界面，操作直观

## 🛠️ 安装步骤

1. **安装依赖包**：
   ```bash
   pip install openai python-dotenv
   ```

2. **配置 Azure OpenAI**：
   编辑 `config.env` 文件，填入你的完整 Azure OpenAI 配置：
   
   ```env
   # Azure OpenAI 完整密钥（请替换为你的真实密钥）
   AZURE_OPENAI_KEY=你的完整API密钥
   
   # Chat 端点配置
   AZURE_OPENAI_ENDPOINT_CHAT=https://你的资源名.openai.azure.com/
   AZURE_OPENAI_DEPLOYMENT_CHAT=你的部署名称
   AZURE_OPENAI_API_VERSION_CHAT=2024-12-01-preview
   ```

3. **运行程序**：
   ```bash
   python pure_memory_chat.py
   ```

## 🎮 使用方法

### 基本对话
- 直接输入你想说的话，AI 会记住并回应
- 例如：
  ```
  你: 我的名字是小明，我喜欢踢足球
  AI: 你好小明！很高兴认识你。踢足球是很好的运动...
  
  你: 你还记得我的爱好吗？
  AI: 当然记得！你说过你喜欢踢足球...
  ```

### 特殊命令
- `quit` / `exit` / `退出` - 退出程序
- `clear` / `清空` - 清空对话记忆
- `info` / `信息` - 查看记忆状态
- `help` / `帮助` - 显示帮助信息

### 程序特点
- 🧠 **智能记忆**：记住你的姓名、偏好、之前讨论的话题
- 💬 **上下文理解**：可以引用之前的对话内容
- 🔄 **持续对话**：像真人聊天一样自然

## ⚠️ 注意事项

1. **API 密钥安全**：
   - 请妥善保管你的 Azure OpenAI API 密钥
   - 不要将包含真实密钥的配置文件提交到公共代码仓库

2. **记忆限制**：
   - 所有对话只保存在内存中
   - 程序关闭后对话历史会丢失
   - 长时间对话可能会达到 token 限制

3. **成本控制**：
   - 每次 API 调用都会产生费用
   - 对话越长，token 消耗越多

## 🔧 故障排除

### 401 认证错误
如果出现 "Access denied due to invalid subscription key" 错误：
1. 检查 `config.env` 中的 `AZURE_OPENAI_KEY` 是否为完整的密钥
2. 确认 Azure OpenAI 资源是否已正确部署
3. 验证端点 URL 是否正确

### 环境变量问题
如果环境变量加载失败：
1. 确保 `config.env` 文件在程序同一目录下
2. 检查文件编码是否为 UTF-8
3. 确认没有多余的空格或特殊字符

### 网络连接问题
如果出现网络错误：
1. 检查网络连接是否正常
2. 确认防火墙设置
3. 验证 Azure 服务是否可用

## 📝 示例配置

下面是一个完整的 `config.env` 示例（请替换为你的真实信息）：

```env
# Azure OpenAI 配置
AZURE_OPENAI_KEY=abcdef1234567890abcdef1234567890abcdef1234567890abcdef1234567890
AZURE_OPENAI_ENDPOINT_CHAT=https://your-resource-name.openai.azure.com/
AZURE_OPENAI_DEPLOYMENT_CHAT=gpt-35-turbo
AZURE_OPENAI_API_VERSION_CHAT=2024-12-01-preview
```

如果你需要获取这些信息，请登录 Azure 门户查看你的 OpenAI 资源详情。
